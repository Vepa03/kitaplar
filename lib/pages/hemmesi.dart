import 'package:flutter/material.dart';

class Hemmesi extends StatelessWidget {
  const Hemmesi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Hemme Kitaplar"),
        elevation: 2,
      ),
    );
  }
}